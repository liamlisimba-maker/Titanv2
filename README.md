# TITAN v2 — Foundation

Multi-user, Telegram-first, capital-preserving trading system.

## Setup
1. Clone the repo
2. Copy .env.example to .env
3. Install dependencies: pip install -r requirements.txt
4. Run: python main.py
